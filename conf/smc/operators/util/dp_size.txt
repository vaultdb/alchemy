package $packageName.$processName;

//naively return input size
int runPublic@v(int$sSize[v] a, int$sSize[v] b) {
	secure int tupleCount;
	getArrElements(a, tupleCount);	

    int dst;

	//insert native method for dp noise here

	dst = 0;
	return dst;
}